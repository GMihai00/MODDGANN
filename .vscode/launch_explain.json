{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Python Script with Args",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/model/explainability.py",
            "args": [
                "--model_type",
                "healthy-unhealthy"
            ],
            "console": "integratedTerminal"
        }
    ]
}